import { Router } from "express";

const router = Router()

router.post("/signup",()=>{})
router.post("/login",()=>{})

router.get("/me",()=>{})

router.put("/update/:id",()=>{})
router.delete("/delete/:id",()=>{})